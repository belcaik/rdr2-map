# Tile source configuration
# "local"  — serve tiles from the backend at localhost:3001 (default, local dev only)
# "none"   — no tiles, markers render on a dark background
VITE_TILE_SOURCE=local
VITE_API_BASE=http://localhost:3001/api
