# Optional legacy tile utility only: export in the shell (not auto-loaded).
# The canonical dataset/media pipeline is configured by CLI arguments, not these vars.
RDR2_RATE_LIMIT=0.5
RDR2_MAX_CONCURRENT=3
